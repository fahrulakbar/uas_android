<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/gradient"
    tools:context="com.myapp.ayatayatsukses.MainInsert">

    <EditText
        android:id="@+id/edit_nama"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@drawable/customborder"
        android:textColor="#000"
        android:backgroundTint="#fff"
        android:hint="Nama Ayat" />

    <EditText
        android:id="@+id/edit_isi"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@id/edit_nama"
        android:background="@drawable/customborder"
        android:layout_marginTop="20dp"
        android:textColor="#000"
        android:backgroundTint="#fff"
        android:hint="Isi Ayat" />

    <EditText
        android:id="@+id/edit_arti"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@id/edit_isi"
        android:layout_marginTop="20dp"
        android:background="@drawable/customborder"
        android:textColor="#000"
        android:backgroundTint="#fff"
        android:hint="Arti Ayat" />

    <Button
        android:id="@+id/btn_lihat"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_arti"
        android:layout_marginTop="40dp"
        android:background="@drawable/customborder"
        android:text="LIHAT"
        android:textColor="#fff"
        android:backgroundTint="#9977DB"
        android:onClick="lihat_insert"
        />

    <Button
        android:id="@+id/tambah"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btn_lihat"
        android:background="@drawable/customborder"
        android:layout_marginTop="20dp"
        android:textColor="#fff"
        android:backgroundTint="#9977DB"
        android:text="TAMBAH" />


</RelativeLayout>
